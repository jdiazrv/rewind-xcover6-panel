Future<List<String>> scanLanForSignalK(int port, {void Function(int checked, int total)? onProgress}) async {
  throw UnsupportedError('Buscar en la red no está disponible en la versión web (el navegador no permite escanear la LAN)');
}
